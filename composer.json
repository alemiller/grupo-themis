{
    "scripts": {
        "compile": [
            "chmod -R 777 /app/app/uploads",
            "chmod -R 777 var/",
            "chmod 755 etc/",
            "chmod 644 etc/config.php"
        ]
    }
}

